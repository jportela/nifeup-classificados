<h1>Listing ad_tags</h1>

<table>
  <tr>
    <th>Tag</th>
    <th>Ad</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ad_tags.each do |ad_tag| %>
  <tr>
    <td><%= ad_tag.tag %></td>
    <td><%= ad_tag.ad %></td>
    <td><%= link_to 'Show', ad_tag %></td>
    <td><%= link_to 'Edit', edit_ad_tag_path(ad_tag) %></td>
    <td><%= link_to 'Destroy', ad_tag, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ad tag', new_ad_tag_path %>
